<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/bg"
    
    android:orientation="vertical" >

    <include
        android:id="@+id/listbar"
        layout="@layout/titlebar_question" />
    <!-- android:listSelector="@drawable/item_selected" -->
   <include
        android:id="@+id/navmenu"     
        layout="@layout/q_nav_bar" 
       
        />
  <android.support.v4.view.ViewPager
            android:id="@+id/pager"
                    android:layout_width="fill_parent"
        android:layout_height="fill_parent"
            android:layout_below="@id/listbar"
        	android:layout_above="@id/navmenu"
           >
    </android.support.v4.view.ViewPager>
   <!-- 
    <com.dt.android.view.MyGallery
        android:id="@+id/gallery"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/listbar"
        android:layout_above="@id/navmenu"
       
        android:gravity="top"
        android:spacing="0dp" />
 -->
 

</RelativeLayout>